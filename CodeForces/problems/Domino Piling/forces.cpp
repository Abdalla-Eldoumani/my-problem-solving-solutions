#include <bits/stdc++.h>
using namespace std;

void solve() {
    int M, N; cin >> M >> N;

    cout << floor(M * N / 2) << '\n';
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}