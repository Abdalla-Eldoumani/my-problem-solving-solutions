#include <bits/stdc++.h>
using namespace std;

inline void solve() {
    int n; cin >> n;
    cout << (n % 2 == 0 ? "Mahmoud\n" : "Ehab\n");
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}