#include <bits/stdc++.h>
using namespace std;

inline void solve() {
    int x; cin >> x;
    cout << (x + 4) / 5 << '\n';
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}